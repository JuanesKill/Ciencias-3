#include <iostream>
#include <cstdlib>
#include <ctype.h>
#include <iomanip>
#include "pila.h"

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

using namespace std;

EquilibrarSimbolos(char cadena){
	pila Pila;
	char cadena1=cadena;
	int n;
	while(n<=100){
		if (cadena[0]=="( : [ "){
			
		}
	}
}

int main(int argc, char** argv) {
	char cadena[100];
	cin>> cadena;
	
	std::cout << cadena[100];
	std::cout << cadena[0];
	return 0;
}
